diff --git a/src/prime_rl/trainer/batch.py b/src/prime_rl/trainer/batch.py
index 706aa2862..ca52f4f69 100644
--- a/src/prime_rl/trainer/batch.py
+++ b/src/prime_rl/trainer/batch.py
@@ -81,8 +81,10 @@ def _dense_ffn_hidden_size(config: Any) -> int:
 def _moe_ffn_hidden_size(config: Any) -> int:
     """Effective FFN width of an MoE layer: routed experts (topk) plus shared experts."""
     dense_ffn = _dense_ffn_hidden_size(config)
-    routed_topk = getattr(config, "moe_router_topk", getattr(config, "num_experts_per_tok", 1))
+    routed_topk = getattr(config, "moe_router_topk", getattr(config, "num_experts_per_tok", 1)) or 1
     moe_ffn = getattr(config, "moe_ffn_hidden_size", getattr(config, "moe_intermediate_size", dense_ffn))
+    if moe_ffn is None:
+        moe_ffn = dense_ffn
     return moe_ffn * routed_topk + (getattr(config, "moe_shared_expert_intermediate_size", None) or 0)
 
 
diff --git a/src/prime_rl/trainer/model.py b/src/prime_rl/trainer/model.py
index 121ba9cb8..5b7b8f70f 100644
--- a/src/prime_rl/trainer/model.py
+++ b/src/prime_rl/trainer/model.py
@@ -776,6 +776,7 @@ def get_model(
                 pretrained_model_name_or_path=config.name,
                 config=model_config,
                 trust_remote_code=config.trust_remote_code,
+                ignore_mismatched_sizes=config.debug.num_layers is not None,
                 **dtype_kwarg,
             )
         logger.debug(f"Loaded model {config.name} in {time.perf_counter() - load_model_start_time:.2f} seconds")
